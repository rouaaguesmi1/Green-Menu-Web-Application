<form method="post" action="{{ path('app_restaurant_delete', {'restaurantid': restaurant.restaurantid}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ restaurant.restaurantid) }}">
	<button class=" btn btn-outline-danger">Delete</button>
</form>
