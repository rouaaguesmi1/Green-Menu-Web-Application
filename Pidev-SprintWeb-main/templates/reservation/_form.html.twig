{{ form_start(form) }}
<div class="form-group fw-bold">
	{{ form_label(form.datetime, 'Reservation Datetime') }}
	{{ form_widget(form.datetime, {'attr': {'class': 'datetimepicker'}}) }}

</div>
<br>
<div class="form-group fw-bold">
	{{ form_label(form.numberofpersons, 'Number of Persons') }}
	{{ form_widget(form.numberofpersons) }}

	<br>
</div>
<div class="form-group">
	<button type="submit" class="btn btn-primary ">Save</button>
</div>


{{ form_end(form) }}
